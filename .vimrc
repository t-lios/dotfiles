" 行番号を表示
:set number
:set title

" ファイルを上書きする前にバックアップ作成を無効化
:set nowritebackup
:set nobackup

" 挿入モードでバックスペースで削除
set backspace=indent,eol,start

" シンタックスハイライトを有効にする
syntax on
colorscheme molokai
set t_Co=256
